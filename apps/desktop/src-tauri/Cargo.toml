[package]
name = "plato-editor"
version = "0.1.0"
description = "Plato Editor desktop shell"
authors = ["Plato Editor"]
edition = "2021"

[lib]
name = "plato_editor_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
