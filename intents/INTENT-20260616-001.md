---
id: INTENT-20260616-001
title: Add orchestrator, governance profiles, config, and state machine
status: done
story_points: 5
retry_count: 0
branch: intent/20260616-001-aidlc-state-machine
---

# Description

Add the newly created AI-DLC orchestrator agent to the project documentation and make the lifecycle executable through a canonical config file, governance profiles, and a CLI-enforced state machine.

# Acceptance Criteria

- [x] `aidlc-orchestrator` is documented in `AIDLC.md` and `AGENTS.md`
- [x] `.aidlc/config.yaml` defines lifecycle transitions, governance profiles, validation commands, and required agents
- [x] `scripts/aidlc.ts` creates and validates the config file
- [x] `scripts/aidlc.ts` exposes a transition command that enforces valid status movement
- [x] `doctor` validates config, agents, and intents
- [x] Validation commands are executed and results are recorded
- [x] CLI can verify and prepare the intent branch before development continues
- [x] Lifecycle docs explain out-of-lifecycle questions/conversations that do not require an intent
- [x] Lifecycle docs explain how to verify Codex model and reasoning configuration
- [x] Tests cover branch verification behavior without mutating the repository under test

# Related Files

- `AGENTS.md`
- `AIDLC.md`
- `.aidlc/config.yaml`
- `.codex/agents/aidlc-orchestrator.toml`
- `.codex/agents/reader.toml`
- `.codex/agents/planner.toml`
- `.codex/agents/builder.toml`
- `.codex/agents/tester.toml`
- `.codex/agents/reviewer.toml`
- `.codex/agents/documenter.toml`
- `scripts/aidlc.ts`
- `package.json`
- `tests/aidlc.test.ts`

# Implementation Notes

- Reader and Planner context gathered from `AIDLC.md`, `AGENTS.md`, existing CLI, package scripts, and the new `.codex/agents/*.toml` files.
- Builder stage started after confirming the new orchestrator agent exists and is currently undocumented.
- Added `.aidlc/config.yaml` as the executable lifecycle config with required agents, validation commands, governance profiles, approval gates, and allowed transitions.
- Replaced the CLI implementation with config loading, config validation, required agent validation, intent validation, and `transition <intent> <target-status>`.
- Updated `AIDLC.md` and `AGENTS.md` to document the orchestrator, config source of truth, governance profiles, approval gates, and CLI-enforced transitions.
- Added package scripts for `aidlc:validate`, `aidlc:config`, and `aidlc:transition`.
- Rework requested after approval:
  - Branch enforcement was missing; the previous cycle happened on `main` even though the intent listed a branch.
  - Read-only/out-of-lifecycle conversations should be explicitly allowed without an intent.
  - Documentation should explain how to verify Codex model and reasoning settings.
- Returned lifecycle status from `approved` to `review`, then to `in_development`, using configured transitions.
- Fast-forwarded branch `intent/20260616-001-aidlc-state-machine` to the prior `main` commit so development now continues on the intent branch.
- Added `branches.enforce_intent_branch` to `.aidlc/config.yaml` and the default generated config.
- Added `bun aidlc:branch <intent>` to report the required branch, current branch, and corrective `git switch` command.
- Added branch enforcement to `context_ready -> in_development`; when enabled, the transition is blocked if the current Git branch differs from the intent branch.
- Updated `AGENTS.md` and `AIDLC.md` with out-of-lifecycle question rules.
- Updated `AGENTS.md` and `AIDLC.md` with Codex model/reasoning verification guidance using `config.toml`, CLI overrides, and runtime logs/OpenTelemetry.

# Test Notes

- `bun scripts/aidlc.ts config` passed and printed the parsed config.
- `bun scripts/aidlc.ts validate` passed.
- `bun scripts/aidlc.ts doctor` passed.
- `bun scripts/aidlc.ts status` passed and reported one `in_development` intent.
- `bun scripts/aidlc.ts transition INTENT-20260616-001 done` failed as expected because `in_development -> done` is not an allowed transition.
- `bun scripts/aidlc.ts transition INTENT-20260616-001 ready_for_testing` passed and reported the `strict` approval gate.
- `bun aidlc:check` passed.
- `bun aidlc:doctor` passed.
- `bun aidlc:config` passed and printed the parsed config.
- `git diff --check` passed with line-ending warnings only.
- Added `tests/aidlc.test.ts` with CLI behavior tests for setup, doctor, missing required agents, invalid transitions, and valid transitions with approval gates.
- `bun test` passed: 5 tests, 0 failures.
- `bun aidlc:doctor` passed after adding the test suite.
- `git diff --check` passed after adding tests, with line-ending warnings only.
- Review found and fixed a section parsing edge case where final Markdown sections were not reliably read.
- Added a regression test for `approved -> done` when `Review Notes` is the final section.
- `bun test` passed after review fix: 6 tests, 0 failures.
- `bun aidlc:doctor` passed after review fix.
- `git diff --check` passed after review fix, with line-ending warnings only.
- `bun test` passed after branch enforcement and docs updates: 8 tests, 0 failures.
- `bun aidlc:branch INTENT-20260616-001` passed and confirmed the current branch matches the intent branch.
- `bun aidlc:doctor` passed after branch enforcement and docs updates.
- `git diff --check` passed after branch enforcement and docs updates, with line-ending warnings only.
- Tester stage rerun after resuming lifecycle:
  - `bun test` passed: 8 tests, 0 failures.
  - `bun aidlc:doctor` passed.
  - `bun aidlc:branch INTENT-20260616-001` passed.
  - `bun aidlc:status` reported one `ready_for_testing` intent.
  - `git diff --check` passed, with line-ending warnings only.
- Review found and fixed a branch preparation gap: the CLI now distinguishes existing branches from missing branches and suggests either `git switch` or `git switch -c`.
- `bun test` passed after review fix: 9 tests, 0 failures.
- `bun aidlc:branch INTENT-20260616-001` passed after review fix and confirmed the branch exists.
- `bun aidlc:doctor` passed after review fix.
- `git diff --check` passed after review fix, with line-ending warnings only.
- Final validation after approval:
  - `bun test` passed: 9 tests, 0 failures.
  - `bun aidlc:doctor` passed.
  - `bun aidlc:status` reported one `approved` intent.
  - `git diff --check` passed, with line-ending warnings only.

# Review Notes

Approved.

- Acceptance criteria are satisfied.
- Tests cover setup, doctor validation, missing required agents, invalid transitions, valid transitions, approval gates, and final-section review notes.
- Documentation and CLI behavior are aligned with `.aidlc/config.yaml`.
- One review issue was found in `getSection` and fixed before approval.

Reopened after approval for additional branch enforcement and documentation improvements.

Approved after rework.

- Acceptance criteria are satisfied.
- Branch enforcement now checks the current branch before development transitions.
- Branch preparation guidance distinguishes existing branches from missing branches.
- Tests cover matching branch, existing-branch mismatch, missing-branch mismatch, and the prior transition behavior.
- Documentation covers out-of-lifecycle questions and Codex model/reasoning verification.

Documenter stage completed.

- `AGENTS.md` documents branch verification, out-of-lifecycle questions, and Codex model/reasoning verification.
- `AIDLC.md` documents branch verification, branch enforcement, out-of-lifecycle work, and Codex model/reasoning verification.
- No additional documentation files were needed for this intent.

# Commits
