---
id: INTENT-20260616-001
title: Add orchestrator, governance profiles, config, and state machine
status: approved
story_points: 5
retry_count: 0
branch: intent/20260616-001-aidlc-state-machine
---

# Description

Add the newly created AI-DLC orchestrator agent to the project documentation and make the lifecycle executable through a canonical config file, governance profiles, and a CLI-enforced state machine.

# Acceptance Criteria

- [x] `aidlc-orchestrator` is documented in `AIDLC.md` and `AGENTS.md`
- [x] `.aidlc/config.yaml` defines lifecycle transitions, governance profiles, validation commands, and required agents
- [x] `scripts/aidlc.ts` creates and validates the config file
- [x] `scripts/aidlc.ts` exposes a transition command that enforces valid status movement
- [x] `doctor` validates config, agents, and intents
- [x] Validation commands are executed and results are recorded

# Related Files

- `AGENTS.md`
- `AIDLC.md`
- `.aidlc/config.yaml`
- `.codex/agents/aidlc-orchestrator.toml`
- `.codex/agents/reader.toml`
- `.codex/agents/planner.toml`
- `.codex/agents/builder.toml`
- `.codex/agents/tester.toml`
- `.codex/agents/reviewer.toml`
- `.codex/agents/documenter.toml`
- `scripts/aidlc.ts`
- `package.json`

# Implementation Notes

- Reader and Planner context gathered from `AIDLC.md`, `AGENTS.md`, existing CLI, package scripts, and the new `.codex/agents/*.toml` files.
- Builder stage started after confirming the new orchestrator agent exists and is currently undocumented.
- Added `.aidlc/config.yaml` as the executable lifecycle config with required agents, validation commands, governance profiles, approval gates, and allowed transitions.
- Replaced the CLI implementation with config loading, config validation, required agent validation, intent validation, and `transition <intent> <target-status>`.
- Updated `AIDLC.md` and `AGENTS.md` to document the orchestrator, config source of truth, governance profiles, approval gates, and CLI-enforced transitions.
- Added package scripts for `aidlc:validate`, `aidlc:config`, and `aidlc:transition`.

# Test Notes

- `bun scripts/aidlc.ts config` passed and printed the parsed config.
- `bun scripts/aidlc.ts validate` passed.
- `bun scripts/aidlc.ts doctor` passed.
- `bun scripts/aidlc.ts status` passed and reported one `in_development` intent.
- `bun scripts/aidlc.ts transition INTENT-20260616-001 done` failed as expected because `in_development -> done` is not an allowed transition.
- `bun scripts/aidlc.ts transition INTENT-20260616-001 ready_for_testing` passed and reported the `strict` approval gate.
- `bun aidlc:check` passed.
- `bun aidlc:doctor` passed.
- `bun aidlc:config` passed and printed the parsed config.
- `git diff --check` passed with line-ending warnings only.
- Added `tests/aidlc.test.ts` with CLI behavior tests for setup, doctor, missing required agents, invalid transitions, and valid transitions with approval gates.
- `bun test` passed: 5 tests, 0 failures.
- `bun aidlc:doctor` passed after adding the test suite.
- `git diff --check` passed after adding tests, with line-ending warnings only.
- Review found and fixed a section parsing edge case where final Markdown sections were not reliably read.
- Added a regression test for `approved -> done` when `Review Notes` is the final section.
- `bun test` passed after review fix: 6 tests, 0 failures.
- `bun aidlc:doctor` passed after review fix.
- `git diff --check` passed after review fix, with line-ending warnings only.

# Review Notes

Approved.

- Acceptance criteria are satisfied.
- Tests cover setup, doctor validation, missing required agents, invalid transitions, valid transitions, approval gates, and final-section review notes.
- Documentation and CLI behavior are aligned with `.aidlc/config.yaml`.
- One review issue was found in `getSection` and fixed before approval.

# Commits
